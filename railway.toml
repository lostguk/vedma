[build]
builder = "DOCKERFILE"
dockerfilePath = "./docker/php/Dockerfile"

[deploy]
startCommand = "PORT=${PORT} php artisan serve --host=0.0.0.0 --port=${PORT}"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
